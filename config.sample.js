// 手動設定する場合はこれを public/config.js にコピーしてキーを記入（Git管理外）。
// 通常は GEMINI_API_KEY 環境変数つきで firebase deploy すれば自動生成されます。
// 認証はヘッダー(x-goog-api-key)方式なので、新形式キー(AQ.…)も旧形式(AIzaSy…)も使えます。
window.GEMINI_API_KEY = "ここにGemini APIキー";
// window.GEMINI_MODEL = "gemini-3.5-flash"; // 任意
